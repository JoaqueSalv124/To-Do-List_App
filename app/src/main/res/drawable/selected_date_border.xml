<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@android:color/transparent" />  <!-- Background remains the same -->
    <stroke android:width="4dp" android:color="@color/selected_date_outline" />  <!-- Outline color -->
    <corners android:radius="10dp" />  <!-- Optional: Rounded corners -->
</shape>
